(env
 (dev
  (flags
   (:standard -w -27)))
 (release
  (flags
   (:standard -w -27))))

(executable
 (name sail_plugin_asl)
 (modes
  (native plugin)
  (byte plugin))
 (libraries libsail))

(install
 (section
  (site
   (libsail plugins)))
 (package sail_asl_backend)
 (files sail_plugin_asl.cmxs sail_plugin_asl.cma))
